/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'plus.unsplash.com', 'aceternity.com', 'assets.aceternity.com'],
    },
};

export default nextConfig;
